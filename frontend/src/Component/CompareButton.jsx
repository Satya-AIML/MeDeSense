// src/Project/Component/CompareButton.jsx

import React from 'react';

const CompareButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>
      Go to Compare
    </button>
  );
};

export default CompareButton;
